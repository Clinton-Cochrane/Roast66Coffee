# Docker Compose environment variables (copy to .env in repo root)
# Used by docker-compose.yml for local development only.
# NEVER use these defaults in production.

POSTGRES_USER=root
POSTGRES_PASSWORD=toor
